rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Block all writes as this is a local-only file app.
      // Allow read if authenticated, though technically no files should exist.
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}
